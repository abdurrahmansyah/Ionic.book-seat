import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hutamakarya.bookseat',
  appName: 'book-seat',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
